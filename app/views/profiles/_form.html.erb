<%= form_tag, url: sign_in_path remote: true do |form| %> 
	<%= form.text_field :financial_goal, value: "RM ", id: "goal-input" %>
	<%= form.submit "➜", class: "btn btn-primary btn-lg outline", id: "goal-submit" %>
<% end %>